# ===== 必填配置 =====
# 大模型配置
# 大模型服务的 API 访问地址（如阿里云、私有部署的模型服务地址）
MIDSCENE_MODEL_BASE_URL=""
# 大模型服务的鉴权密钥（由模型服务提供，用于身份验证）
MIDSCENE_MODEL_API_KEY=""
# 使用的具体模型名称（示例：qwen3-vl-plus）
MIDSCENE_MODEL_NAME=""
# 模型所属的系列（示例：qwen3-vl，用于能力和特性分组）
MIDSCENE_MODEL_FAMILY=""

# 应用标识配置
APP_ID_ANDROID="com.a.b"
APP_ID_IOS="com.daimler.ris.mercedesme.cn.ios"

# ===== 可选配置 =====
# 可选，模型响应的语言；如果当前系统时区是 GMT+8 则默认是 Chinese，否则是 English
MIDSCENE_PREFERRED_LANGUAGE="Chinese"

# 日志级别配置
LOG_LEVEL=debug